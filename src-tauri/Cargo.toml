[package]
name = "gestion-escritorio"
version = "0.1.24"
description = "Aplicación de escritorio y estación de impresión de Gestión"
authors = ["Gestión"]
edition = "2021"

[lib]
name = "gestion_escritorio_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
